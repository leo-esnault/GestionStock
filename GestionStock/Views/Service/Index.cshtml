@model IEnumerable<Service>
@{
    ViewData["Title"] = "Index";
}

<partial name="_Notification" />

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Liste des services</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Service" asp-action="Creation" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Créer un Service
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Nom
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td width="95%">
                    @obj.Nom
                </td>
                <td width="5%">
                    <div class="w-20 btn-group" role="group">
                        <a asp-controller="Service" asp-action="Suppression" asp-route-nom="@obj.Nom" class="btn btn-danger btn-sm mx-1"> <i class="bi bi-trash3-fill"></i></a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

